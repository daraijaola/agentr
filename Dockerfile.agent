FROM node:20-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 python3-pip curl bash procps \
 && apt-get clean && rm -rf /var/lib/apt/lists/*

RUN npm install -g pm2

RUN useradd -m -u 1001 agent

USER agent
WORKDIR /workspace

CMD ["sleep", "infinity"]
